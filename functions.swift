// Fibonacci Problem
func fibonacci(numToPrint: Int) {

}

// Factorial Recursive
// I understand the concept, but I am having trouble actually forming the
// algorithm in my head to make this work :/ I do know what they are and
// how they work, jsut can't seem to make them myself.
func factorial_recursive(num: Int) {
  if num == 1 {

  }
  else {
    factorial_recursive(num: num - 1)
  }
}
